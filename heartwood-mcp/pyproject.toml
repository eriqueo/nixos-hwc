[project]
name = "heartwood-mcp"
version = "0.1.0"
description = "JobTread MCP server for Heartwood Craft"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
heartwood-mcp = "server:main"

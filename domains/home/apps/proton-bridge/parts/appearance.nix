# ProtonMail Bridge • Appearance part
# Minimal appearance configuration (Bridge is headless).
{ lib, pkgs, config, ... }:

{
  files = profileBase: {
    # Bridge runs headless, no appearance configuration needed
  };
}
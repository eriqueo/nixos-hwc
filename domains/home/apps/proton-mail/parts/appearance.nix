# ProtonMail • Appearance part
# Theming and visual configuration.
{ lib, pkgs, config, osConfig ? {}, ... }:

{
  files = profileBase: {
    # ProtonMail desktop client uses system theming
    # No specific configuration files needed
  };
}
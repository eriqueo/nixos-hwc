# TEMPLATE: Navidrome Admin Password
# 
# To create the actual encrypted secret:
# 1. Install age if not already: nix-shell -p age
# 2. Encrypt your Navidrome password:
#    echo -n 'your-secure-password-here' | age -R /etc/age/keys.txt.pub > navidrome-admin-password.age
# 3. Delete this .TEMPLATE file after creating the real .age file
#
# Current hardcoded password in profiles/server.nix: il0wwlm?
# Consider using a different, more secure password when encrypting

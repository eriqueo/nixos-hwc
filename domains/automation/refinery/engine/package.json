{
  "name": "@refinery/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Substance-agnostic refinement engine core: Item state machine, contracts, profile loader, gate registry, effectors, stage runner.",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "rm -rf dist && tsc -p tsconfig.json && node --test --enable-source-maps 'dist/test/**/*.test.js'"
  },
  "dependencies": {
    "yaml": "2.5.1",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "22.7.5",
    "typescript": "5.6.3"
  }
}

You are analyzing Facebook group posts from the "JobTread Pros" group.
JobTread is a construction project management platform used by remodeling contractors.
DataX is a data and analytics platform built on top of JobTread.

Classify each post. Return ONLY valid JSON — no markdown, no explanation, no preamble.

CLASSIFICATIONS (pick exactly one):
- "warm_lead"          — Someone actively looking for a tool, report, integration, or capability
                         that DataX provides. They want something and don't have it yet.
- "pain_point"         — A contractor describing friction, a manual workaround, or a broken
                         workflow. Any expressed frustration with how data flows (or doesn't).
- "migration_signal"   — Someone considering leaving JobTread, evaluating alternatives,
                         or frustrated enough with the platform to be at risk of churning.
- "feature_request"    — Something missing or broken in JobTread itself (not a third-party
                         integration need) — a native feature gap.
- "competitor_mention" — Mentions of software used alongside or instead of JobTread:
                         QuickBooks, Zapier, ConstructConnect, Buildertrend, Excel, etc.
- "general"            — Everything else: tips, how-tos, celebrations, questions with no
                         friction signal, announcements.

BIAS RULES:
- If a post expresses any friction or manual work → prefer pain_point over general
- If a post mentions any third-party tool → prefer competitor_mention over general
- If a post asks for something JobTread doesn't do → prefer feature_request over general
- Reserve "general" for posts with zero friction or gap signal

TAGS — pick all that apply:
estimating, reporting, analytics, integration, ai_usage, invoicing,
scheduling, field_ops, lead_management, subcontractors, cash_flow, budget_tracking, quickbooks

NOTIFY: Set to true for warm_lead, pain_point, migration_signal, competitor_mention, feature_request.
Set to false for general only.

SUMMARY: One sentence stating the problem or need. Be factual and specific.
- Good: "Contractor manually exports job data to Excel weekly because JT has no scheduled report export."
- Good: "GC looking for a way to track budget vs actual hours per employee across jobs."
- Bad: "This contractor struggles with reporting and DataX could help them."
- Bad: "User is frustrated with JobTread's limitations."
For general posts, use empty string.

Return ONLY this JSON structure — every input post_id must appear in results:
{
  "results": [
    {
      "post_id": "<post_id from input>",
      "classification": "<one of the six classifications>",
      "tags": ["<tag>"],
      "notify": true,
      "summary": "<one factual sentence or empty string>"
    }
  ]
}

POSTS TO CLASSIFY:

# Today-queue dispatch — refinery item triage (read-only)

You are a READ-ONLY diagnostic agent on hwc-server. Allowed: file reads under
/var/lib/refinery, ~/.nixos, ~/900_vaults/brain; journalctl for the refinery
services; rg/glob. Forbidden: any write, any POST to the board, any state change.

Refinery item: {{TITLE}}
State: {{WHY}}
Board link: {{URL}}

1. Read the item's .md card in /var/lib/refinery/items (id: {{ID}} — strip the
   `refinery:` prefix) — payload, gate history, error output.
2. If FAILED: find the exact gate error, locate the code/schema that produced
   it, and state the minimal fix (file:line if possible).
3. If PARKED: restate the decision being asked in one sentence, list the 2-3
   options with one-line tradeoffs, and recommend one.
4. If READY-TO-PROMOTE: summarize what was shaped, flag anything the premortem
   gate raised, and state what promoting will kick off.

Write the report to the REPORT_PATH in this card's header. End with NEXT
ACTION: the single verb Eric should dispatch (run / resume / promote / delete)
and why. Under 60 lines, findings first.

# Immich Container - System Lane Configuration
# All container configuration in parts/config.nix
{ lib, config, pkgs, ... }:

{
  # No system-lane configuration needed for immich container
  # Everything handled in parts/config.nix
}

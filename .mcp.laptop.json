{
  "mcpServers": {
    "_filesystem_DISABLED": {
      "_comment": "Disabled: npx causes npm to hang and spike CPU to 200%",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/home/eric/.nixos",
        "/etc/nixos",
        "/home/eric/.config"
      ]
    },
    "git": {
      "command": "uvx",
      "args": ["mcp-server-git", "--repository", "/home/eric/.nixos"]
    },
    "_brave-search_DISABLED": {
      "_comment": "Disabled: npx causes npm to hang and spike CPU to 200%",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
      }
    },
    "_github_DISABLED": {
      "_comment": "Disabled: npx causes npm to hang and spike CPU to 200%",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {

      }
    },
    "_sequential-thinking_DISABLED": {
      "_comment": "Disabled: npx causes npm to hang and spike CPU to 200%",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    },
    "time": {
      "command": "uvx",
      "args": ["mcp-server-time"]
    },
    "fetch": {
      "command": "uvx",
      "args": ["mcp-server-fetch"]
    },
    "_memory_DISABLED": {
      "_comment": "Disabled: npx causes npm to hang and spike CPU to 200%",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
